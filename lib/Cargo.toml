[package]
name = "heic-shim"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
heic = "0.1.6"

[profile.release]
opt-level = 3
lto = true
panic = "abort"
strip = true
